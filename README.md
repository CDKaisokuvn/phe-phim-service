# PHE PHIM SCRAPING SERVICE

To run:

- `yarn` or `npm install`
- `yarn dev` or `npm run dev` to run in development mode
- `yarn build` or `npm run build` to build
- `yarn start` or `npm start` to run in production mode
